# Demo Authentication Token
# In production, use proper authentication (NextAuth.js, Clerk, Auth0, etc.)
DEMO_AUTH_TOKEN=demo-token-123

# Set to 'true' to show admin link (for demo purposes)
# In production, use proper session/user role checks
NEXT_PUBLIC_SHOW_ADMIN=true
